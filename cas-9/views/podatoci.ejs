<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=`, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <%- include("header") %>
    <h1>Zdravo <%= ime; %></h1>
    <ul>
      <% if(studenti.length > 5){ %> <% studenti.forEach(s => { %>
      <li><%= s.firstName %> <%= s.lastName %></li>
      <% }) %> <%} else { %>
      <p>Nema dovolno studenti</p>
      <% } %>
    </ul>
  </body>
</html>
